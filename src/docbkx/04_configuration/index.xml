<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:xi="http://www.w3.org/2001/XInclude" 
	xsi:schemaLocation="http://docbook.org/ns/docbook http://docbook.org/xml/5.0/xsd/docbook.xsd"
	xml:id="chap_configuration">

	<title>Configuration</title>

	<para>
	Eclipse SCADA consists of many different configuration options. Therefore the whole
    configuration process is split up into several parts.
	</para>
  
  <para>
  The first layer is the actual configuration required by the different software components.
  This can be custom XML files, system properties or the CA mechanism. 
  </para>
  
  <para>
  The second layer is the <quote>world model</quote>, which wraps up all different configurations
  of the first layer and capture it in an EMF model, unless the first layer already is an EMF model.
  </para>
  
  <para>
  The third layer is the <quote>component/infrastructure model</quote>. It defines the system
  infrastructure (nodes, applications, etc, …) and the different components (sensor value, namespace, etc, …).
  </para>
  
  <para>
  In addition there are tool models that aid in processing the configuration. Like the recipes or the
  globalization model. 
  </para>
  
    <xi:include href="03_cim/index.xml"/>
    <xi:include href="04_recipes/index.xml"/>    
	
</chapter>
